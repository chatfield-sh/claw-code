[project]
name = "shai-core-api"
version = "0.1.0"
description = "SHAI Core — domain-neutral executive OS backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
